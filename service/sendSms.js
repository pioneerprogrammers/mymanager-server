const sendPhoneOtp = (options) => {
  const {} = options;
  console.log("====================================");
  console.log("Got the data to sent otp to phone", options);
  console.log("====================================");

  return;
};

module.exports = { sendPhoneOtp };
